apiVersion: v1
kind: Secret
metadata:
  name: cloudpulse-secrets
  namespace: cloudpulse
  labels:
    app.kubernetes.io/name: cloudpulse
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  # Example secret keys (Base64 or stringData template)
  # Replace with sealed-secrets, external-secrets-operator, or vault integration in production
  DATABASE_AUTH_KEY: "change-me-in-production-vault"
  PAYMENT_GATEWAY_TOKEN: "sandbox-jwt-auth-token"
  ALERT_WEBHOOK_URL: "https://hooks.slack.com/services/T000/B000/XXXX"
